Java：
    输入说明：输入仅有一行，由0~9、+、-、(、)、{、}和【空格】组成，要求【手工输入】而不是文件读入；


              正确的输入由若干多项式及其之前或两者之间的一个正负号还有若干【空格】组成；
              相邻两个多项式之间有且仅有一个+或-，第一个多项式之前可能存在正负号（若有，则仅有一个）；
              每个多项式的任意两个数对的n都不相同。

              待计算多项式形如{(c1,n1),(c2,n2),…,(cm,nm)}：
                  最外端由花括号包含；
                  内部是若干由括号引起的数对；
                  括号之间由一个【逗号】分隔，两个数之间也由一个【逗号】分隔；
                  对于数对(c,n)，c为系数，n为幂，-10^8<c<10^8, -10^6<n<10^6；
                  输入的多项式的各项间不必有先后次序关系；
                  {}表示空多项式，即值为0；
                  输入范式内【可任意添加空格】；
                  不满足以上任意一条要求的输入均认定为非法输入。
    输出说明：若输出“Wrong input format! Retype your data!”则表示用户非法输入；
              正确输入后得到的结果多项式形如{ (c1,n1) (c2,n2) … (cm,nm) }：
                  最外端由花括号包含；
                  内部是若干由括号引起的数对；
                  括号之间由一个【空格】分隔，两个数之间由一个【逗号】分隔；
                  对于数对(c,n)，c为系数，n为幂，-10^8<c<10^8, -10^6<n<10^6；
                  结果多项式的各项间无先后次序关系；
                  {}表示空多项式，即值为0。

C：
    输入说明：输入仅有一行，由0~9、+、-、(、)、{、}组成，要求【手工输入】而不是文件读入；


              正确的输入由若干多项式及其之前或两者之间的一个正负号组成；
              相邻两个多项式之间有且仅有一个+或-，第一个多项式之前可能存在正负号（若有，则仅有一个）；
              每个多项式的任意两个数对的n都不相同。

              待计算多项式形如{(c1,n1),(c2,n2),…,(cm,nm)}：
                  最外端由花括号包含；
                  内部是若干由括号引起的数对；
                  括号之间由一个【逗号】分隔，两个数之间也由一个【逗号】分隔；
                  对于数对(c,n)，c为系数，n为幂，-10^8<c<10^8, -10^6<n<10^6；
                  输入的多项式的各项间不必有先后次序关系；
                  {(0,0)}表示空多项式，即值为0；
                  输入范式内【不可添加空格】；
                  不满足以上任意一条要求的输入均认定为非法输入。
    输出说明：正确输入后得到的结果多项式形如{ (c1,n1) (c2,n2) … (cm,nm) }：
                  最外端由花括号包含；
                  内部是若干由括号引起的数对；
                  括号之间由一个【空格】分隔，两个数之间由一个【逗号】分隔；
                  对于数对(c,n)，c为系数，n为幂，-10^8<c<10^8, -10^6<n<10^6；
                  结果多项式的各项间无先后次序关系；
                  {}表示空多项式，即值为0。